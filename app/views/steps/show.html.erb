<div id='step'>
  <h2><%= @step.description %></h2>
  <%= simple_format(@step.description) %>
</div>
<%= link_to "Edit Step", [:edit, @topic, @step] %>
<%= link_to "Delete Step", [@topic, @step], :method => :delete,
       :confirm => "Are you SURE you want to delete this step?" %>
